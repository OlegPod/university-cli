spring:
  datasource:
    url: jdbc:mysql://localhost:3306/university?&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: university_user
    password: pass
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: university_database_schema.sql