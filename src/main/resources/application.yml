spring:
  datasource:
    url: jdbc:mysql://localhost:3306/university?&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: university_user
    password: pass
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: oleh_database_create.sql